// import { connectDB } from '../DB/mongoose';
// import { MONGO_URL } from '../utils/environment';

// connectDB(MONGO_URL)
//   .then(() => mongoose.connection.db.dropDatabase())
//   .then(() => {
//     console.log('Connected to test db');
//   });
